<Window x:Class="MusicManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:MusicManager.Views"
        Title="MusicManager"
        Width="960" Height="660"
        MinWidth="800" MinHeight="520"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource ForegroundBrush}">

    <Window.Resources>
        <Style TargetType="StatusBarItem">
            <Setter Property="Foreground" Value="{StaticResource ForegroundMutedBrush}" />
            <Setter Property="FontSize"   Value="12" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Tab Control -->
        <TabControl Grid.Row="0"
                    x:Name="MainTabControl"
                    Margin="0">

            <!-- Download Tab -->
            <TabItem Header="⬇  Download">
                <views:DownloadTab />
            </TabItem>

            <!-- Playlist Tab -->
            <TabItem Header="🎵  Playlist">
                <views:PlaylistTab />
            </TabItem>

            <!-- Device Tab -->
            <TabItem Header="💾  Dispositivo">
                <views:DeviceTab x:Name="DeviceTabView" />
            </TabItem>

        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="1"
                   Background="{StaticResource SurfaceBrush}"
                   BorderBrush="{StaticResource BorderBrush}"
                   BorderThickness="0,1,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Pronto." />
            </StatusBarItem>
            <Separator />
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="DeviceStatusText" Text="Nessun dispositivo collegato" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
